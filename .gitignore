node_modules/
dist/
release-artifacts/
build/icon.icns
src/web/static/tray.png
*.tsbuildinfo
.claude/
.ai/
.codex/
.idea/
.reasonix/
*.iml

# Public documentation is versioned by language. Maintainer development notes
# and any future top-level documentation workspace remain local-only.
/docs/*
!/docs/en/
!/docs/en/**
!/docs/ch/
!/docs/ch/**
/logs/

# Local skill and rule layers — generated by `skill-central init` and edited by
# `add` / `install` / the web board. Per-user and per-project personal digital
# assets, not part of the published package. New users get a fresh scaffold on
# `init`.
.skills/
.rules/

# Per-project engine config (paths + priorities). Same reasoning.
# skill-central.yaml
