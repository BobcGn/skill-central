node_modules/
dist/
release-artifacts/
*.tsbuildinfo
.claude/
.ai/
.codex/
.idea/
*.iml

# Private project notes and execution evidence. Keep local; never publish.
/docs/
/logs/

# Local skill layers — generated by `skill-central init` and edited by
# `add` / `install` / the web board. Per-user and per-project, not part
# of the published package. New users get a fresh scaffold on `init`.
# .skills/

# Per-project engine config (paths + priorities). Same reasoning.
# skill-central.yaml
